import 'package:flutter/material.dart';

class AppTabletScaffold extends StatelessWidget {
  const AppTabletScaffold({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
